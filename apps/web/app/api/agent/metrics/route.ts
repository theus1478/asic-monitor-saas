export async function POST() {
  // A implementação final valida o token do agente e grava no Supabase.
  return Response.json({ error: "O endpoint do agente será ativado ao conectar o Supabase." }, { status: 503 });
}
